#include <iostream>
#include <stdio.h>
#include <bits/stdc++.h>
#include <string>

using namespace std;

int main()
{
    int s,t,d;
    scanf("%d %d %d", &s,&t,&d);
    printf("%d", d/(s*2)*t);
    return 0;
}
